interface:
  display_name: "Steve CC Skill"
  short_description: "Instrumentation selection, calculations, standards, reports"
  default_prompt: "Use $steve-cc-skill to evaluate an industrial instrument selection with documented sources, calculations, risks, and missing conditions."

policy:
  allow_implicit_invocation: true
